class Solution:
    def isPalindrome(self, x: int) -> bool:
        s=str(x)
        if s[0] == '-':
            return False
        else:
            a = int(s[::-1])
            if a == x:
                return True
            else:
                return False
